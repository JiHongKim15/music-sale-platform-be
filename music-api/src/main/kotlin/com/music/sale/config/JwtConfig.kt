package com.music.sale.config

import org.springframework.context.annotation.Configuration

@Configuration
open class JwtConfig {
    // This class can be used for JWT related configurations if needed
}
